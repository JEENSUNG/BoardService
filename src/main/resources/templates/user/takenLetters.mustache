{{>layout/header}}
<div id="posts_list">
    <table id="table" class="table table-horizontal">
        <thead id="thead">
        <tr>
            <th>받는사람</th>
            <th>제목</th>
            <th>날짜</th>
            <th>삭제</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#letters}}
            <tr>
                <input type="hidden" id="id" value="{{id}}">
                <td>{{takenUsername}}</a></td>
                <td><a href="/letters/{{id}}/send">{{title}}</a></td>
                <td>{{createdDate}}</td>
                <td>
                    <button type="button" id="btn-delete-letter2" class="btn btn-danger">삭제</button>
                </td>
            </tr>
        {{/letters}}
        </tbody>
    </table>
    {{>posts/posts-page}}
</div>
{{>layout/footer}}