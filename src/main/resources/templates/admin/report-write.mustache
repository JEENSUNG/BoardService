{{>layout/header}}
<div id="posts_list">
    <div class="container col-md-8">
        <form>
            <input type="hidden" id="userId" value="{{user.id}}">
            <input type="hidden" id="pageNum" value="{{pageNum}}">
            <div class="form-group">
                {{nickname}}의 게시글에 대한 신고<br>
                <label for="title">제목</label>
                <input type="text" class="form-control" id="title" placeholder="제목을 입력하세요">
            </div>
            <div class="form-group">
                <label for="content">내용</label>
                <textarea rows="15" class="form-control" id="content" placeholder="내용을 상세하게 입력하세요"></textarea>
            </div>
        </form>
        <button type="button" id="btn-post-report-save" class="btn btn-primary bi bi-pencil-fill"> 작성</button>
    </div>
</div>
{{>layout/footer}}